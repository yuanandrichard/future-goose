# Netlify 部署配置
[build]
  command = "npm run build"
  publish = "dist"

# Functions 目录配置
[functions]
  directory = "netlify/functions"

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
